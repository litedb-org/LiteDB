name: CI

on:
  pull_request:

jobs:
  build-and-test:
    if: ${{ false }} # Temporarily disabled for ReproRunner debugging
    uses: ./.github/workflows/_reusable-ci.yml

  repro-runner:
    uses: ./.github/workflows/reprorunner.yml
    secrets: inherit
