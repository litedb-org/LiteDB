# Explicit #1002 / #2811 behavioral co-repair

Prepared from `646ce03488f6e54ebbf0e98e33b71aaba122ec93`. This revises only the #1002 execution contract and preserves the other twenty-seven. It authorizes nine exact behavioral regressions to turn green under one new immutable campaign. It does not change frozen tests, production code, failure normalization, known-failure classes or source-guard policy.

## Why combine these reports

The #1002 null nullable Int32 path and every #2811 typed write path share RemoveDocId/GetBsonDocs in Insert.cs. A narrow null-only guard could avoid one new symptom while leaving the same read-only auto-ID defect in non-nullable Int32/ObjectId. A general guard necessarily changes #2811 outcomes, so that work must be explicit and fully validated rather than accepted as failure drift.

The first v8 candidate `a98b29089c01e21e50856abd0cc9bbea1ecc5631` wrote a get-only nullable-ID row before throwing NullReferenceException. Lifecycle review executed that case and observed immediate and reopened count one. The second candidate `d600cab9aa8be419f0d3069a21bb5e03ff713307` checked Setter before writes but threw InvalidOperationException. Its eight changed #2811 failures are a partial repair and remain rejected. The preserved evidence fixture includes this negative candidate and the full original lifecycle finding.

A new worker must start from current integration with these original obligations, fresh CI and three reviews. Preserve the old campaign journal, candidates, changed-failure verdict and findings; do not rewrite its runtime/base/contract or accept attempt 2. The new runtime/campaign must identify this supersession explicitly.

## Exact evidence

[issue-1002-2811-corepair.json](../../../scripts/bugfix/fixtures/issue-1002-2811-corepair.json) preserves test IDs, full canonical failure text and hashes, source blobs, the former two-case contract, the new contract, six original artifact identities with baseline/repeat raw hashes, and current failed-attempt evidence.

All eleven case identities and canonical outcomes match in twelve reports from original run [34988724926](https://github.com/litedb-org/LiteDB/actions/runs/34988724926): six dd937719 baseline reports and six unrelated #2874 repeat-candidate reports. They also match the integration baseline from [35013331767](https://github.com/litedb-org/LiteDB/actions/runs/35013331767), source `bbb0253bc06324f0bb14a21a727a37e8c7f2b213`, original tests `dd937719f7eee53c512f50ac604cab639bf42a4c`, workflow `61ee4aec705024c189345de25508ea19ffc2584e`. All thirteen focused baseline checks pass under the revised contract.

The latest artifact is ID `10414383642`, `bugfix-check-ubuntu-latest-net8.0`; retained archive SHA-256 `7b0da04ddcd26e833abe41b0f1e5c51940d3e1d386c6d5c61ab93a6439db5af1`. Baseline TRX SHA-256 is `e0ae57ecf1cc31504f8bb6cfce31fcde06b798ba63ccc723f0300754b19e75ad`; rejected candidate TRX SHA-256 is `28bcc9ce79cc4aada88b7b41d19b90ec70e900c6759757bb6a516a2703e09a0e`. The contract uses the existing environment-aware full-failure schema, with all six actual evidence environments. All classifications are identical across these environments.

No new normalization was added. The existing reviewed exact ObjectId/InsertOne normalization replaces its generated ObjectId in failure output. Its required match count and every other assertion remain unchanged. All other new target cases are literal full canonical failures. The contract now rejects changed failure tails as well as changed first lines.

## Controls and acceptance

| Selection | Baseline | Requirement after repair |
| --- | --- | --- |
| #1002 nullable Int32 null | 1 red | Generated unique persisted Int32 keys and correct entity copy-back |
| #1002 nullable Int32 zero | 1 green control | Remains green with reopen/lookup checks |
| #2811 Int32/ObjectId × InsertOne/InsertMany/UpsertOne/UpsertMany | 8 red | Every complete frozen atomicity case passes |
| AutoId_Tests.AutoId_Strong_Typed | 1 green control | Writable IDs, insert/enumeration, update/upsert, explicit IDs and sequences stay valid |

#2811 itself first seeds an explicit-ID immutable object and independently checks its bytes and control row before the failing operation. On rejection, it requires exact LiteException type, a message naming Id and explaining read-only/setter status, only the original control row, and byte-identical durable storage. Single insert currently fails both error and atomicity assertions; enumerable paths currently fail the diagnostic assertions. The fixture also permits coherent success for immutable entities; it does not require assigning a get-only property. Reviewers must describe the chosen behavior honestly.

The separate AutoId_Strong_Typed control executes ordinary Int32/Int64/Guid/ObjectId generation, string explicit IDs, multi-insert, update and upsert, database lookups, row counts and numeric sequence assertions. It is not a vacuous marker or an embedded assertion reached only after the defect is fixed. Both controls pass in all thirteen baseline/repeat reports.

The frozen #2811 models do not include a nullable get-only ID or a throwing custom setter. Those gaps remain mandatory lifecycle review obligations, not implied coverage from passing the selected tests. The broad gate still rejects unrelated unexpected passes, classification changes and permanent passing-case regressions.

## Scope and compact profile

Filter: `FullyQualifiedName~LiteDB.Tests.Issues.Issue1002_|FullyQualifiedName~LiteDB.Tests.Issues.Issue2811_|FullyQualifiedName=LiteDB.Tests.Database.AutoId_Tests.AutoId_Strong_Typed`.

Production scope remains only `LiteDB/Client/Database/Collections/Insert.cs`. Upsert(T) delegates to its enumerable overload, which uses GetBsonDocs from that file; explicit-ID overloads bypass auto-ID removal. No separate Upsert.cs edit is needed for the observed shared-helper repair. Any necessary scope expansion must be justified and reviewed separately. Original audit-source-guards.json contains no Insert.cs or Upsert.cs observation entry.

Require Ubuntu net8 and net10 focused+broad, production builds and the existing #1002 compatibility profile. Broad already runs the AutoId_Tests class; do not duplicate it. No full matrix is required per repair. All six observed environments remain approved; runtime requirements and changed contract hashes bind the new profile.

Frozen files:

- `LiteDB.Tests/Issues/Issue1002_Tests.cs`: `300112d2fa9c10055460484549e551f7d750451e`.
- `LiteDB.Tests/Issues/Issue2811_Tests.cs`: `408f4c3df519e6e446bb9e15fdfd63da0580e95d`.
- `LiteDB.Tests/Database/AutoId_Tests.cs`: `0e2eed9f88e95e29623b5f99122b294cac1e9787`.

Exact target identities and full canonical SHA-256:

- `LiteDB.Tests.Issues.Issue1002_Tests.Nullable_integer_empty_ids_generate_unique_persisted_integer_keys(empty: null)` — test ID `6a6112a1-4157-a1d3-8b5c-a7b3b813631b`, failure `f3d319412038c304a9098a8cf3ad28dd538eb40c2597d6157de42c0a0538f02d`.
- `LiteDB.Tests.Issues.Issue2811_Tests.Read_only_int_auto_id_is_atomic_for_every_typed_write_path(path: InsertMany)` — test ID `a11c7fca-c6e4-e44f-29e8-b7b5edb09f22`, failure `edf61022a483c6e7f47903b8d1c6dc957d9f967e08d59d92164b1c8b8e174393`.
- `LiteDB.Tests.Issues.Issue2811_Tests.Read_only_int_auto_id_is_atomic_for_every_typed_write_path(path: InsertOne)` — test ID `608569d7-80ea-7a1d-e39f-c78ea6ecb853`, failure `a18833b0eb1db770bb9ebab375a85e8a4db2d227e6dff857077ad0ffb1b23cad`.
- `LiteDB.Tests.Issues.Issue2811_Tests.Read_only_int_auto_id_is_atomic_for_every_typed_write_path(path: UpsertMany)` — test ID `96dd1933-af03-2f82-6ee4-ea0721e3c82f`, failure `edf61022a483c6e7f47903b8d1c6dc957d9f967e08d59d92164b1c8b8e174393`.
- `LiteDB.Tests.Issues.Issue2811_Tests.Read_only_int_auto_id_is_atomic_for_every_typed_write_path(path: UpsertOne)` — test ID `e0969e3f-fa89-09d7-54f9-e38645cbec46`, failure `edf61022a483c6e7f47903b8d1c6dc957d9f967e08d59d92164b1c8b8e174393`.
- `LiteDB.Tests.Issues.Issue2811_Tests.Read_only_object_id_is_atomic_for_every_typed_write_path(path: InsertMany)` — test ID `8be49034-0623-7625-28a1-48c6cf715fe8`, failure `edf61022a483c6e7f47903b8d1c6dc957d9f967e08d59d92164b1c8b8e174393`.
- `LiteDB.Tests.Issues.Issue2811_Tests.Read_only_object_id_is_atomic_for_every_typed_write_path(path: InsertOne)` — test ID `3a8fb504-bfa3-ffb4-95ae-3baa644297ec`, failure `5b94968784d53f07bc4179500f75f6e1816be075499e94446afe65220319d8db`.
- `LiteDB.Tests.Issues.Issue2811_Tests.Read_only_object_id_is_atomic_for_every_typed_write_path(path: UpsertMany)` — test ID `ae91b9c9-cec3-f8aa-c18d-3440de9c8702`, failure `edf61022a483c6e7f47903b8d1c6dc957d9f967e08d59d92164b1c8b8e174393`.
- `LiteDB.Tests.Issues.Issue2811_Tests.Read_only_object_id_is_atomic_for_every_typed_write_path(path: UpsertOne)` — test ID `f0f942d7-b1cc-a333-fc83-9cce15239425`, failure `edf61022a483c6e7f47903b8d1c6dc957d9f967e08d59d92164b1c8b8e174393`.

## Obligations delivered in task.json

- **behavior**: This is an explicit behavioral co-repair of #1002 and all eight frozen #2811 cases, not permission for changed failure classifications. Settable nullable Int32 null and zero IDs must generate unique persisted integer keys and copy them back. Read-only Int32/ObjectId auto-ID InsertOne, InsertMany, UpsertOne and UpsertMany must either succeed coherently or fail before writes with a LiteException naming Id and explaining read-only/setter status. Preserve explicit-ID writes to immutable objects and ordinary writable Int32/Int64/Guid/ObjectId auto-generation; AutoId_Strong_Typed is a real independent passing control.
- **behavior**: Keep the shared RemoveDocId/GetBsonDocs behavior consistent across Insert and Upsert. Do not broaden rejection to assigned IDs or mapped documents that do not need copy-back. No source-guard disposition, failure normalization extension or class-drift allowance is authorized.
- **compatibility**: Preserve stored BSON key types, numeric sequence behavior, uniqueness, entity copy-back and existing explicit-ID rows. Inspect single and enumerable Insert/Upsert, including the obsolete InsertBulk wrapper; all reach the shared Insert.cs helpers except explicit-ID overloads. The frozen #2811 successful branch permits retaining a get-only entity ID unchanged, so explain the chosen public behavior instead of claiming it necessarily gains writable-ID semantics. Preserve ordinary v8 reopen and compatibility checks; no format change is authorized.
- **lifecycle**: Mandatory prior finding from expansion-v8-1002 candidate a98b29089c01e21e50856abd0cc9bbea1ecc5631: a model with public int? Id { get; } and null ID threw NullReferenceException after Insert had committed; immediate and reopened counts were both one. Reproduce this exact get-only nullable-ID case on the new candidate and demonstrate either acknowledged coherent success or rejection with no durable write. Frozen #2811 uses non-nullable int and ObjectId, so passing it alone does not close this finding. Do not drop this obligation when starting a superseding campaign.
- **lifecycle**: Validate writability before any engine mutation that can commit; never repair atomicity by write-then-delete compensation. Provide evidence for implicit and explicit transactions, caller Commit after catching rejection, unchanged pre-existing rows, and immediate plus independently reopened state. Check mixed/lazy batches with valid earlier items and an invalid later item: no prefix from a failed write call may remain commit-able or durable. Explain rollback effects on prior explicit-transaction work and avoid claiming per-call rollback preserves earlier work without evidence.
- **lifecycle**: Attempt d600cab9aa8be419f0d3069a21bb5e03ff713307 only changed the error to InvalidOperationException and still failed all eight #2811 cases; it is not an accepted base or a completed repair. Use the current integrated base and fresh focused+broad evidence, retaining all original findings through three new reviews.
- **lifecycle**: Assess every newly reachable post-engine ID copy-back failure, including a non-null custom setter that throws. A Setter != null guard alone does not prove safety for newly supported null nullable IDs. Demonstrate that no newly supported input can produce a durable failed call because assignment occurs after commit; review both single and enumerable paths and require rollback-safe placement or an equivalent justified design.

## Validation design

Adversarial tests reject attempt 2, partial green results, changed failure tails, skipped/missing/extra cases, changed IDs, failed positive controls and unrelated source-guard passes. Historical preservation checks use a test-only hash-pinned amendment so only the reviewed #1002 contract can change; its original two-case evidence remains tested separately. Every other issue contract stays exact. No CI, worker or remote action was launched for preparation.

Completed local suites: **129 gate tests, 218 controller/profile tests and 87 workflow/helper tests passed**. Thirteen actual original/repeat/current-integration baseline gates passed; the second candidate remains rejected.
