# Issue 2578: [BUG] BsonCtor not called

Source: https://github.com/litedb-org/LiteDB/issues/2578 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2578_Tests.BsonCtor_receives_stored_id_for_overridden_get_only_properties`
  - Expected loaded.UsedBsonCtor to be True, but found False.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2578_`.
