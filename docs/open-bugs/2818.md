# Issue 2818: Committed transactions are lost on power loss or OS crash: commits never fsync the log file, and encrypted databases never fsync at all

Source: https://github.com/litedb-org/LiteDB/issues/2818 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 2}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2818_Tests.Commit_flushes_log_to_disk_and_durable_snapshot_recovers_exact_rows(password: "secret")`
  - Expected log.DurableFlushes to be greater than 0 because commit must request durable flush before reporting success, but found 0.
- Failed: `Issue2818_Tests.Commit_flushes_log_to_disk_and_durable_snapshot_recovers_exact_rows(password: null)`
  - Expected log.DurableFlushes to be greater than 0 because commit must request durable flush before reporting success, but found 0.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2818_`.
