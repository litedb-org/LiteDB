# Issue 1904: [BUG] Quering Based On Referenced Entity Failed After Creating The Index

Source: https://github.com/litedb-org/LiteDB/issues/1904 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue1904_Tests.Adding_reference_field_index_cannot_remove_included_matches`
  - Expected col.Include(x => x.Homes).Find(Query.Any().EQ("$.Homes[*].Value", 65)).Select(x => x.Id).OrderBy(x => x) to be equal to {1, 3}, but found empty collection.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue1904_`.
