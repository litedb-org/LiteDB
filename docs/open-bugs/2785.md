# Issue 2785: A leftover or foreign -log.db file is replayed into a new or different data file without any identity check

Source: https://github.com/litedb-org/LiteDB/issues/2785 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 2}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2785_Tests.Foreign_WAL_cannot_replace_existing_or_new_database_contents(existing: False)`
  - Expected error to be <null>, but found Xunit.Sdk.XunitException: Expected victim.GetCollectionNames() to be a collection with 0 item(s), but {"foreign"}
- Failed: `Issue2785_Tests.Foreign_WAL_cannot_replace_existing_or_new_database_contents(existing: True)`
  - Expected error to be <null>, but found Xunit.Sdk.XunitException: Expected victim.GetCollectionNames()[0] to be "own" with a length of 3, but "foreign" has a length of 7, differs near "for" (index 0).

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2785_`.
