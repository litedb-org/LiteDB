# Issue 1908: [BUG] Cannot support Linq expression with array index

Source: https://github.com/litedb-org/LiteDB/issues/1908 (open on 2026-09-12).

## Contract and independent checks

Compare array-index predicates with a materialized CLR scan, change the index and captured array contents, and include a missing value.

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1}. Assertions describe correct behavior; failures are not marked skipped or inverted.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue1908_`.
