# Issue 2802: Overridden BsonMapper.ToObject is never called when reading through ILiteCollection<T> / ILiteQueryable<T> in v5

Source: https://github.com/litedb-org/LiteDB/issues/2802 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 5}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2802_Tests.Typed_read_uses_virtual_mapper_and_returns_its_result(api: "Find")`
  - Expected result.Name to be "decoded:one" with a length of 11 because calling and discarding the override's result is insufficient, but "one" has a length of 3, differs near "one" (index 0).
- Failed: `Issue2802_Tests.Typed_read_uses_virtual_mapper_and_returns_its_result(api: "FindAll")`
  - Expected result.Name to be "decoded:one" with a length of 11 because calling and discarding the override's result is insufficient, but "one" has a length of 3, differs near "one" (index 0).
- Failed: `Issue2802_Tests.Typed_read_uses_virtual_mapper_and_returns_its_result(api: "FindById")`
  - Expected result.Name to be "decoded:one" with a length of 11 because calling and discarding the override's result is insufficient, but "one" has a length of 3, differs near "one" (index 0).
- Failed: `Issue2802_Tests.Typed_read_uses_virtual_mapper_and_returns_its_result(api: "FindOne")`
  - Expected result.Name to be "decoded:one" with a length of 11 because calling and discarding the override's result is insufficient, but "one" has a length of 3, differs near "one" (index 0).
- Failed: `Issue2802_Tests.Typed_read_uses_virtual_mapper_and_returns_its_result(api: "Query")`
  - Expected result.Name to be "decoded:one" with a length of 11 because calling and discarding the override's result is insufficient, but "one" has a length of 3, differs near "one" (index 0).

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2802_`.
