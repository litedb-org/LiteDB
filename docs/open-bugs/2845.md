# Issue 2845: JsonSerializer doesn't round-trip doubles: ~15 significant digits, double.MaxValue becomes Infinity, then null

Source: https://github.com/litedb-org/LiteDB/issues/2845 (open on 2026-09-12).

## Contract and independent checks

BSON control and three JSON exports must preserve exact IEEE-754 bits and BSON type, including extrema, subnormals and integral doubles.

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 7, 'Passed': 1}. Assertions describe correct behavior; failures are not marked skipped or inverted.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2845_`.
