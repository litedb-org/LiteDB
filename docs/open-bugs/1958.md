# Issue 1958: [BUG] Rebuild cause "LiteDB ENSURE: Use EnterTransaction() before EnterLock(name) "

Source: https://github.com/litedb-org/LiteDB/issues/1958 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue1958_Tests.Rebuild_cannot_discard_another_threads_transaction_or_poison_followup_writes`
  - Expected owner.Result to be <null>, but found Xunit.Sdk.XunitException: Expected db.Commit() to be True because rebuild must not silently discard the active transaction, but found False.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue1958_`.
