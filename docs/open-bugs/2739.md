# Issue 2739: [BUG] collection.UpdateMany stores property with DbRef inline instead of as reference

Source: https://github.com/litedb-org/LiteDB/issues/2739 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2739_Tests.UpdateMany_stores_a_reference_and_later_includes_current_target_data`
  - Expected reference.Keys[0] to be "$id", but "_id" differs near "_id" (index 0).

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2739_`.
