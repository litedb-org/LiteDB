# Issue 2784: Opening a file that is not a valid LiteDB v5 datafile throws ArgumentOutOfRangeException "Ticks must be between..." instead of an invalid-database error

Source: https://github.com/litedb-org/LiteDB/issues/2784 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 2}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2784_Tests.Invalid_header_with_out_of_range_creation_time_returns_format_error(sqliteHeader: False)`
  - Expected type to be LiteDB.LiteException, but found System.ArgumentOutOfRangeException.
- Failed: `Issue2784_Tests.Invalid_header_with_out_of_range_creation_time_returns_format_error(sqliteHeader: True)`
  - Expected type to be LiteDB.LiteException, but found System.ArgumentOutOfRangeException.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2784_`.

## Manual sweep verification

The #2820 fix (`4cfed472`) also resolves this report. HeaderPage now validates the
signature/version before decoding CreationTime, and DiskService validates the
header before any tail repair. Both original #2784 cases pass, with exact input
bytes unchanged. The combined #2784/#2820 fixture has 13 passing cases on net8.0.
No additional production change is needed for the reproduced invalid-signature
files; corruption in otherwise valid headers is outside these two cases.
