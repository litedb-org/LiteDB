# Issue 2816: "The process cannot access the file because it is being used by another process": data file stays locked after Dispose fails to open the -log.db file

Source: https://github.com/litedb-org/LiteDB/issues/2816 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Passed': 2}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Passed: `Issue2816_Tests.Read_then_dispose_with_foreign_log_handle_releases_data_handle(mode: Direct)`
- Passed: `Issue2816_Tests.Read_then_dispose_with_foreign_log_handle_releases_data_handle(mode: Shared)`

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2816_`.
