# Issue 1899: Collection.include(x => x.document).find(Query.all()... ) return null 

Source: https://github.com/litedb-org/LiteDB/issues/1899 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue1899_Tests.Paged_QueryAll_preserves_include_and_uses_current_referenced_data`
  - Expected rows.Select(x => x.Document.Name) to be equal to {"fresh 2", "fresh 3"}, but {<null>, <null>} differs at index 0.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue1899_`.
