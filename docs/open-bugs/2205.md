# Issue 2205: [BUG] Value was too small or to large int64 when using BsonExpression

Source: https://github.com/litedb-org/LiteDB/issues/2205 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1, 'Passed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Passed: `Issue2205_Tests.Huge_numeric_looking_strings_are_preserved_when_quoted_or_parameterized`
- Failed: `Issue2205_Tests.Out_of_range_unquoted_numeric_token_matches_the_complete_string_value`
  - Creating the expression throws `System.OverflowException` before the exact
    stored string can be matched. The test requires the one complete match and
    rejects prefix/suffix distractors; merely wrapping the overflow in a
    `LiteException` does not satisfy the reported behavior.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2205_`.
