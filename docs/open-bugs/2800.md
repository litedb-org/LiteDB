# Issue 2800: Queries with nested lambdas (FILTER/MAP/array filter) return the first execution's results when re-run with different captured values

Source: https://github.com/litedb-org/LiteDB/issues/2800 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 3}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2800_Tests.Nested_expression_parameters_are_fresh_across_queries_and_databases(source: "COUNT($.Items[@ = @0]) > 0")`
  - Expected col.Find(expression).Select(x => x["_id"].AsInt32).OrderBy(x => x) to be equal to {2}, but {1} differs at index 0.
- Failed: `Issue2800_Tests.Nested_expression_parameters_are_fresh_across_queries_and_databases(source: "COUNT(FILTER($.Items[*] => @ = @0)) > 0")`
  - Expected col.Find(expression).Select(x => x["_id"].AsInt32).OrderBy(x => x) to be equal to {2}, but {1} differs at index 0.
- Failed: `Issue2800_Tests.Nested_expression_parameters_are_fresh_across_queries_and_databases(source: "MAP($.Items[*] => @ = @0) ANY = true")`
  - Expected col.Find(expression).Select(x => x["_id"].AsInt32).OrderBy(x => x) to be equal to {2}, but {1} differs at index 0.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2800_`.
