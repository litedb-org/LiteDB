# Issue 2549: [BUG] Regression 5.0.17 -> 5.0.18, Database.Rebuild() not disposing correctly

Source: https://github.com/litedb-org/LiteDB/issues/2549 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2549_Tests.Rebuild_publishes_caller_owned_stream_before_database_disposal`
  - Expected rows to contain a single item, but the collection is empty.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2549_`.
