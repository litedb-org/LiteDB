# Issue 2583: [BUG] "Order By <computed_colum>" in SQL query doesn't have effect.

Source: https://github.com/litedb-org/LiteDB/issues/2583 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 2}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2583_Tests.Ordering_by_computed_alias_sorts_values_without_losing_rows(direction: "asc")`
  - Expected source amounts `{30, 12, 0, -5}` after ordering by the
    inverse computed alias, but the rows remain in insertion order.
- Failed: `Issue2583_Tests.Ordering_by_computed_alias_sorts_values_without_losing_rows(direction: "desc")`
  - Expected source amounts `{-5, 0, 12, 30}`, but the rows remain in
    insertion order. Because `100 - amount` reverses the raw-field order, a
    resolver that silently sorts by `amount` cannot satisfy either case.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2583_`.
