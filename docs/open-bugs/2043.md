# Issue 2043: [BUG] Cannot insert duplicate key in unique index '_id'

Source: https://github.com/litedb-org/LiteDB/issues/2043 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Passed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Passed: `Issue2043_Tests.Concurrent_Int64_auto_ids_match_document_ids_and_continue_after_rebuild_and_reopen`

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2043_`.

## Historical comparison (2026-09-15)

The original 5.0.8 package passes the concurrent Int64 auto-ID, document-ID, rebuild, and reopen control (one case). A matching historical version therefore does not establish the missing damaged sequence or concurrent lifecycle that created it.

[Captured test outcomes](evidence/2043-20260915.json).

## Retained bulk/timer lifecycle (2026-09-15)

A closer control now retains the usage described in the reporter's August 10
comment: a single background `Insert(IEnumerable)` writer, transactional Timer
cleanup using predicate Find/Count/descending paginated Query/Delete, two ordered
readers, and FileStorage in a separate database. The unit test runs a smaller
fixture; the [historical runner](../../LiteDB.ReproRunner/Repros/Issue_2043_BulkLifecycle/README.md)
starts with 1,792 rows in a **60.25 MB database**, then acknowledges 768 additional
bulk inserts and independently records every successful deletion. IDs start at
31,000 and must equal the expected sequence throughout, including the recovery
insert with ID 33,560 after reopen.

**5.0.8 and dev each pass three fresh-process attempts on Linux/.NET 8.**
Every attempt verifies all surviving IDs/payloads, deleted-key absence, secondary
index results, transaction-consistent ordered pages, and all 24 stored files.
The complete external ledger is checked before and after two rebuilds, after
reopen and a recovery write, and after another read-only reopen that must preserve
all database bytes. These successful controls do not prove the reported failure.

Rebuild explicitly drains active operations using an application-level lock.
Whether the reporter rebuilt without synchronization is unknown; the private
file, full model, connection string, and original storage history are still
missing. The issue remains unconfirmed.

[Captured six-process comparison](evidence/2043-bulk-lifecycle-local-20260915.json).

[Final Windows 2022 and Ubuntu 22.04/24.04 CI](https://github.com/litedb-org/LiteDB/actions/runs/34955909408)
passes **3/3 fresh-process attempts per variant per platform** with the final
predicate-result-count and minimum-reader-population assertions included. All
platforms use .NET 8. This adds eighteen successful process-level controls and
leaves the original duplicate-auto-ID report unconfirmed.
[Captured platform reports](evidence/2043-bulk-lifecycle-ci-20260915.json).
