# Issue 2770: [BUG] [Crash / unchecked exception] Enum property comparison can abort LINQ translation with EnumAsInteger=false

Source: https://github.com/litedb-org/LiteDB/issues/2770 (open on 2026-09-12).

## Contract and independent checks

Four-row equality/inequality truth table and constant-query control under both enum mapping modes.

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1, 'Passed': 1}. Assertions describe correct behavior; failures are not marked skipped or inverted.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2770_`.
