# Issue 1545: [BUG]BsonExpression with IGrouping.Key acts an unexpect behavior

Source: https://github.com/litedb-org/LiteDB/issues/1545 (open on 2026-09-12).

## Contract and independent checks

Compare each captured IGrouping.Key query with original CLR groups, traversing forward and backward.

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1}. Assertions describe correct behavior; failures are not marked skipped or inverted.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue1545_`.
