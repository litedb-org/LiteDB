# Issue 2807: Parameterize Query helpers to avoid recompiling distinct literal values

Source: https://github.com/litedb-org/LiteDB/issues/2807 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 11}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2807_Tests.Helper_reuses_source_but_keeps_independent_parameter_values(operation: "Between")`
  - Expected first.Source to be "$.value BETWEEN "b" AND "bz"" because different values must not require recompilation, but "$.value BETWEEN "a" AND "az"" differs near "a" " (index 17).
- Failed: `Issue2807_Tests.Helper_reuses_source_but_keeps_independent_parameter_values(operation: "Contains")`
  - Expected first.Source to be "$.value LIKE "%b%"" because different values must not require recompilation, but "$.value LIKE "%a%"" differs near "a%"" (index 15).
- Failed: `Issue2807_Tests.Helper_reuses_source_but_keeps_independent_parameter_values(operation: "EQ")`
  - Expected first.Source to be "$.value="b"" because different values must not require recompilation, but "$.value="a"" differs near "a"" (index 9).
- Failed: `Issue2807_Tests.Helper_reuses_source_but_keeps_independent_parameter_values(operation: "EndsWith")`
  - Expected first.Source to be "$.value LIKE "%b"" because different values must not require recompilation, but "$.value LIKE "%a"" differs near "a"" (index 15).
- Failed: `Issue2807_Tests.Helper_reuses_source_but_keeps_independent_parameter_values(operation: "GT")`
  - Expected first.Source to be "$.value>"b"" because different values must not require recompilation, but "$.value>"a"" differs near "a"" (index 9).
- Failed: `Issue2807_Tests.Helper_reuses_source_but_keeps_independent_parameter_values(operation: "GTE")`
  - Expected first.Source to be "$.value>="b"" because different values must not require recompilation, but "$.value>="a"" differs near "a"" (index 10).
- Failed: `Issue2807_Tests.Helper_reuses_source_but_keeps_independent_parameter_values(operation: "In")`
  - Expected first.Source to be "$.value IN ["b","ba"]" because different values must not require recompilation, but "$.value IN ["a","aa"]" differs near "a"," (index 13).
- Failed: `Issue2807_Tests.Helper_reuses_source_but_keeps_independent_parameter_values(operation: "LT")`
  - Expected first.Source to be "$.value<"b"" because different values must not require recompilation, but "$.value<"a"" differs near "a"" (index 9).
- Failed: `Issue2807_Tests.Helper_reuses_source_but_keeps_independent_parameter_values(operation: "LTE")`
  - Expected first.Source to be "$.value<="b"" because different values must not require recompilation, but "$.value<="a"" differs near "a"" (index 10).
- Failed: `Issue2807_Tests.Helper_reuses_source_but_keeps_independent_parameter_values(operation: "Not")`
  - Expected first.Source to be "$.value!="b"" because different values must not require recompilation, but "$.value!="a"" differs near "a"" (index 10).
- Failed: `Issue2807_Tests.Helper_reuses_source_but_keeps_independent_parameter_values(operation: "StartsWith")`
  - Expected first.Source to be "$.value LIKE "b%"" because different values must not require recompilation, but "$.value LIKE "a%"" differs near "a%"" (index 14).

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2807_`.
