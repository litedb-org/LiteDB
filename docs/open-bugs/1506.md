# Issue 1506: [SOLVED but...] Query.Limit is ignored by Find(Query)

Source: https://github.com/litedb-org/LiteDB/issues/1506 (open on 2026-09-14).

## Observed on dev

Base: `4eca573513de1e7e95fa72b74a954fb179eb728f`; Release, .NET 8, Linux.
`{'Failed': 1, 'Passed': 1}`. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Passed: `Issue1506_Tests.Find_honors_query_limit_when_optional_paging_is_omitted`
  - Replays the original `Offset = 0`, `Limit = 1` report and verifies the exact first row and payload.
- Failed: `Issue1506_Tests.Find_paging_override_preserves_the_complete_reusable_query`
  - `Find(query, skip: 2, limit: 2)` changes the caller's `Offset` from 1 to 2 before returning.
  - The mutation remains visible when the lazy iterator is created, after partial and full enumeration, and on reuse.
  - Exact projected rows independently exercise the query's filter, descending order, select expression, and DbRef include; a replacement query containing only paging values cannot satisfy them.

Run: `dotnet test LiteDB.Tests/LiteDB.Tests.csproj -c Release -f net8.0 -p:TestingEnabled=true --no-restore --settings tests.runsettings --filter FullyQualifiedName~Issue1506_`.
