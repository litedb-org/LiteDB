# Issue 1506: [SOLVED but...] Query.Limit is ignored by Find(Query)

Source: https://github.com/litedb-org/LiteDB/issues/1506 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue1506_Tests.Find_honors_query_paging_and_does_not_mutate_reusable_query`
  - Expected query.Offset to be 1 because a later use must retain the caller's query settings, but found 3.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue1506_`.
