# Issue 2052: [BUG] how to show json string when DB file is broken with Datetime ArgumentOutOfRangeException

Source: https://github.com/litedb-org/LiteDB/issues/2052 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 2}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2052_Tests.Malformed_BSON_date_is_rejected_without_modifying_input_or_hiding_valid_dates(milliseconds: -9223372036854775808)`
  - Expected type to be LiteDB.LiteException because invalid stored BSON needs a database-format error rather than an unstructured DateTime overflow, but found System.ArgumentOutOfRangeException.
- Failed: `Issue2052_Tests.Malformed_BSON_date_is_rejected_without_modifying_input_or_hiding_valid_dates(milliseconds: 9223372036854775807)`
  - Expected type to be LiteDB.LiteException because invalid stored BSON needs a database-format error rather than an unstructured DateTime overflow, but found System.ArgumentOutOfRangeException.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2052_`.
