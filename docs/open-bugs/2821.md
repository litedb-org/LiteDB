# Issue 2821: After a disk-full IOException the LiteDatabase instance stays unusable, and some failure paths leak file handles

Source: https://github.com/litedb-org/LiteDB/issues/2821 (open on 2026-09-12).

## Current repro: the closed-engine diagnostic

The cleanup/reopen tests below pass after the resource cleanup fixes. They did
not cover the issue's still-misleading error on the same instance after storage
recovers. `Closed_instance_explains_recovery_after_a_transient_disk_full_failure`
now injects exactly one log-write failure, verifies that the fault has cleared,
and calls `Count()` on the closed instance. The subsequent error must explain
that the engine is closed or needs reopening and retain the original I/O cause.
Automatically continuing an invalid engine is not required.

Before asserting the diagnostic, the test disposes/reopens twice, verifies that
the original committed record survived and the failed record did not, and
persists a new record. This independently checks the documented recovery path.

On `dev` at `a50661a9`, the recovery controls pass, but `Count()` rethrows only
`injected disk full` after the injected fault has cleared. Validation on Linux
/.NET 8: **one repro failed, two cleanup/recovery controls passed**.

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Passed': 2}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Passed: `Issue2821_Tests.Failed_initialization_releases_the_file_without_finalization`
- Passed: `Issue2821_Tests.Reopening_after_disk_full_preserves_commits_and_accepts_new_writes_without_deleting_WAL`

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2821_`.
