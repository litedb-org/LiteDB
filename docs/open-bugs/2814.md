# Issue 2814: Log file (-log.db) grows without bound when other transactions are always open, because auto-checkpoint is skipped instead of retried

Source: https://github.com/litedb-org/LiteDB/issues/2814 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2814_Tests.Finite_concurrent_readers_do_not_allow_WAL_growth_far_beyond_checkpoint_budget`
  - Expected peak to be less than or equal to 6553600L because auto-checkpoint needs bounded escalation under continuous short readers, but found 8503296L (difference of 1949696).

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2814_`.

## Current-dev repeatability

On dev `a50661a9`, the full-suite run failed the WAL peak assertion, but a later
issue-only run passed without any source change. This is a timing-sensitive
stress repro, not a deterministic proof on every invocation. The independent
reopen checks remain mandatory when the peak assertion passes. A single green
run does not establish that auto-checkpoint starvation was fixed; the coverage
inventory records this issue separately from consistently failing regressions.

## Repeated package/current observations (2026-09-15)

The saved 10-attempt run reproduced the WAL peak failure in **1/10 attempts on
5.0.11 and 10/10 on dev `a50661a9`**. The revised test checks every committed
payload after reopen **before** the growth assertion, so a peak failure cannot
hide incorrect write results. Earlier same-source passes remain valid evidence
of intermittency; do not mark this fixed from a single green invocation.

[Runner](../../LiteDB.ReproRunner/Repros/Issue_2814_WalGrowth/README.md),
[captured observations](evidence/2814-20260915.json).

## Full CI matrix (2026-09-15)

At PR head `ed866e54`, the same growth assertion failed in **all 21 jobs**:
Linux x64/ARM64, macOS, and Windows x64/x86 across .NET 8, 9, and 10.
Observed peaks ranged from 8,142,848 to 48,119,808 bytes against the
6,553,600-byte limit. Every run reached that assertion after the reader and
reopened-payload checks. Earlier local passes still make this an intermittent
repro; the CI matrix does not establish a universal failure rate.

[Per-job results](evidence/2814-ci-matrix-20260915.json).
