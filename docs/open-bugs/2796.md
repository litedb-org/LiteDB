# Issue 2796: Docs: document size limit is 16MB per root document (sub-documents included, BsonRef targets not), but the FileStorage page still says 1MB

Source: https://github.com/litedb-org/LiteDB/issues/2796 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Passed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Passed: `Issue2796_Tests.Root_limit_includes_embedded_documents_but_not_reference_targets`

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2796_`.

## Executed public-documentation comparison (2026-09-15)

`python3 scripts/regressions/issue_2796.py` first passes the independent runtime
root-size/embedded-document/DbRef controls, then reads the public FileStorage and
BsonDocument pages. The former still claims 1MB twice while the latter and the
runtime agree on 16MB. The script exits 1 with `BUG_2796_CONFIRMED`; unavailable
or changed control content is a harness error (exit 2), never a fix.
This is a reproduced **documentation** defect, not a failing storage limit.

## Reviewed website candidate (2026-09-17)

Commit ed1248568 on `codex/manual-2796-docs` corrects the Markdown source,
rendered FileStorage page, docs landing summary and both RSS feeds. Four fresh
Sol-high reviewers approve the final candidate; runtime size controls pass.
The live website is unchanged pending a separate publication decision.
