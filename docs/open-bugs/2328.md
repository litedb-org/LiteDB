# Issue 2328: [BUG] Serialize double.MaxValue

Source: https://github.com/litedb-org/LiteDB/issues/2328 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Passed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Passed: `Issue2328_Tests.Custom_registered_double_max_value_survives_storage_and_SQL_without_null`

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2328_`.

## Historical comparison (2026-09-15)

The original 5.0.16 package passes the custom registered double.MaxValue storage and SQL control (one case). The retained test now follows the reporter’s property/value shape and checks exact persisted IEEE-754 bits and the supplied deserialization path. The missing application export/read path remains a limit; the separate JSON defect is not substituted.

[Captured test outcomes](evidence/2328-20260915.json).

On 2026-09-15, unauthenticated requests to both original Google Drive screenshot
links returned HTTP 404. The screenshots therefore cannot currently resolve
the missing application read/export path; the evidence file records both URLs
and their observed status.
