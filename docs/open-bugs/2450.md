# Issue 2450: [BUG] Rebuild function never deletes the previous database backup

Source: https://github.com/litedb-org/LiteDB/issues/2450 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2450_Tests.Repeated_rebuild_keeps_one_current_readable_backup_and_preserves_unrelated_files`
  - Expected backups to contain a single item because old rebuild backups must not accumulate, but found {"/tmp/litedb-2450-26871239ecd240e8b033adb64daa9bb9/data-backup-1.db", "/tmp/litedb-2450-26871239ecd240e8b033adb64daa9bb9/data-backup.db"}.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2450_`.
