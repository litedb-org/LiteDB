# Issue 2787: Shared mode across threads: "Object synchronization method was called from an unsynchronized block of code" and hangs from mutex released on the wrong thread or never released

Source: https://github.com/litedb-org/LiteDB/issues/2787 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2787_Tests.Shared_transaction_releases_mutex_while_owner_thread_is_still_alive`
  - Expected finishedBeforeOwnerExit to be True because OS abandonment after owner exit is not a correct mutex release, but found False.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2787_`.
