# Issue 2792: "ENSURE: page type must be collection page" / "Invalid Collection on 0 ... Full zero: True" when a collection is read while another transaction is creating it

Source: https://github.com/litedb-org/LiteDB/issues/2792 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2792_Tests.Reader_during_collection_header_publication_never_sees_unconfirmed_page`
  - Expected writer.Result to be <null>, but found System.ObjectDisposedException: Cannot access a closed file.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2792_`.
