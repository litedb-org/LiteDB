# Issue 2815: ReadOnly=true Direct connection interferes with a database another process is writing (checkpoint write on close, blocked -log.db delete, torn reads)

Source: https://github.com/litedb-org/LiteDB/issues/2815 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2815_Tests.Readonly_close_does_not_attempt_checkpoint_or_modify_live_writer_files`
  - Expected errors to be empty because swallowing an attempted read-only checkpoint is not a fix, but found at least one item {System.NotSupportedException: Stream does not support writing.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2815_`.
