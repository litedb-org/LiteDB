# Issue 2822: Explicit transactions are bound to the OS thread, so BeginTrans/Commit/Rollback across await act on the wrong transaction

Source: https://github.com/litedb-org/LiteDB/issues/2822 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2822_Tests.Commit_from_another_thread_does_not_silently_report_no_transaction`
  - Expected committed to be True because silently returning false hides an outstanding transaction after a thread hop, but found False.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2822_`.
