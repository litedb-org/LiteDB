# Issue 2590: [BUG] InvalidCastException From LiteDB.ObjectId To System.String

Source: https://github.com/litedb-org/LiteDB/issues/2590 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2590_Tests.Empty_string_id_does_not_throw_after_committing_an_unaddressable_document`
  - Expected (failure is LiteException || failure is ArgumentException) to be True because an unsupported string auto-id must be validated before the engine writes, but found False.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2590_`.
