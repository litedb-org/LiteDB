# Issue 2847: LINQ string.Equals(value, StringComparison) ignores the comparison and uses the database collation (Ordinal matches case-insensitively)

Source: https://github.com/litedb-org/LiteDB/issues/2847 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 2, 'Passed': 2}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2847_Tests.Explicit_string_comparison_agrees_with_CLR_even_with_an_index(comparison: Ordinal, indexed: False)`
  - Expected col.Find(predicate).Select(x => x.Id).OrderBy(x => x) to be equal to {2}, but {1, 2, 3} contains 2 item(s) too many.
- Failed: `Issue2847_Tests.Explicit_string_comparison_agrees_with_CLR_even_with_an_index(comparison: Ordinal, indexed: True)`
  - Expected col.Find(predicate).Select(x => x.Id).OrderBy(x => x) to be equal to {2}, but {1, 2, 3} contains 2 item(s) too many.
- Passed: `Issue2847_Tests.Explicit_string_comparison_agrees_with_CLR_even_with_an_index(comparison: OrdinalIgnoreCase, indexed: False)`
- Passed: `Issue2847_Tests.Explicit_string_comparison_agrees_with_CLR_even_with_an_index(comparison: OrdinalIgnoreCase, indexed: True)`

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2847_`.
