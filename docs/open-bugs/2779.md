# Issue 2779: LINQ predicates throw on method calls that don't depend on the lambda parameter (string interpolation, Distinct() over a captured list) instead of evaluating them client-side

Source: https://github.com/litedb-org/LiteDB/issues/2779 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
The hardened focused rerun produced `{'Failed': 5, 'Passed': 2}`. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2779_Tests.Captured_method_calls_match_CLR_and_follow_changed_values(operation: "concat")`
  - System.NotSupportedException : Method Concat(string,string,string) in String are not supported when convert to BsonExpression (Concat(value(LiteDB.Tests.Issues.Issue2779_Tests+<>c__DisplayClass1_0).part, ".", value(LiteDB.Tests.Issues.Issue2779_Tests+<>c__DisplayClass1_0).other)).
- Failed: `Issue2779_Tests.Captured_method_calls_match_CLR_and_follow_changed_values(operation: "distinct")`
  - System.NotSupportedException : Method Distinct() in Enumerable are not supported when convert to BsonExpression (value(LiteDB.Tests.Issues.Issue2779_Tests+<>c__DisplayClass1_0).names.Distinct()).
- Failed: `Issue2779_Tests.Captured_method_calls_match_CLR_and_follow_changed_values(operation: "format")`
  - System.NotImplementedException : The method or operation is not implemented.
- Failed: `Issue2779_Tests.Captured_method_calls_match_CLR_and_follow_changed_values(operation: "join")`
  - System.NotImplementedException : The method or operation is not implemented.
- Failed: `Issue2779_Tests.Original_selectmany_distinct_contains_is_one_parameter_and_matches_id_ledger`
  - The original `allstocks.SelectMany(...).Distinct().Contains(e.MobileId)` shape throws for `Distinct()` before it can produce a query plan or results.
- Passed: `Issue2779_Tests.Precomputed_contains_control_uses_the_same_parameter_and_id_ledger`
  - The documented workaround translates to `ITEMS(@p0) ANY=$.MobileId`, binds exactly one `p0` array, exposes `$.MobileId IN ARRAY(ITEMS(@p0))` in `GetPlan()`, and returns ids 11, 22, and 33 while excluding id 44.
- Passed: `Issue2779_Tests.Generic_client_evaluation_and_parameter_dependent_resolver_calls_keep_their_boundaries`
  - A parameter-independent generic client method is called exactly once and bound as `@p0`; a supported row-dependent `ToUpper()` remains server-side, while an unsupported row-dependent client method remains rejected.

The failing original-repro test requires the same one-parameter expression and plan as the passing precomputed control, verifies the exact de-duplicated parameter ledger `99, 7, 42, 777`, and checks a hand-written result-id ledger. This prevents a fix that merely suppresses the exception, drops the predicate, or special-cases the visible result rows from passing.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2779_`.
