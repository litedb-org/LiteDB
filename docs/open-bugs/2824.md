# Issue 2824: Rebuild() on an encrypted database: parameterless call leaves a plaintext file ("File is not encrypted"), password change throws "Invalid password"

Source: https://github.com/litedb-org/LiteDB/issues/2824 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 2}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2824_Tests.Encrypted_rebuild_keeps_or_changes_password_without_losing_instance_or_data(changePassword: False)`
  - Expected failure to be <null>, but found LiteDB.LiteException: File is not encrypted.
- Failed: `Issue2824_Tests.Encrypted_rebuild_keeps_or_changes_password_without_losing_instance_or_data(changePassword: True)`
  - Expected failure to be <null>, but found LiteDB.LiteException: Invalid password.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2824_`.
