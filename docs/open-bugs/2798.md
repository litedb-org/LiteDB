# Issue 2798: LINQ queries on GetCollection<IInterface> ignore [BsonId] declared on the implementing class and silently match nothing

Source: https://github.com/litedb-org/LiteDB/issues/2798 (open on 2026-09-12).

## Observed on dev

Base: `90788bace880f477c9fb52ff7c0da98c9700bdfe`; Release, .NET 8, Linux.
{'Failed': 1}. Assertions describe correct behavior; failures are not skipped or inverted.

## Executed cases

- Failed: `Issue2798_Tests.Interface_queries_and_deletes_use_the_same_id_mapping_as_insert`
  - Expected col.Find(x => x.Key == "alpha").Select(x => x.Value) to be equal to {11}, but found empty collection.

Run: `dotnet test LiteDB.Tests -c Release -f net8.0 -p:TestingEnabled=true --settings tests.runsettings --filter FullyQualifiedName~Issue2798_`.
